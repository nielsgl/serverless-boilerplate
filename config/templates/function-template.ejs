const debug = require('debug')('app:log:src:<%= handlerFunction %>');
const error = require('debug')('app:err:src:<%= handlerFunction %>');

//  functionName
// < functionPath >
// < handlerName %>
// <%= handlerFunction %>

const <%= handlerFunction %> = async (event, context, cb) => {
	const response = {
		statusCode: 200,
		body: JSON.stringify({
			status: 'ok',
		}),
	};

	return cb(null, response);
};

export default <%= handlerFunction %>;
